syntax = "proto3";

package gateway.v1;

import "buf/validate/validate.proto";
import "gateway/v1/gateway.proto";

message GatewayCreated {
  // The created gateway
  gateway.v1.Gateway gateway = 1 [(buf.validate.field).required = true];
}